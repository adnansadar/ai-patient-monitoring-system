export const metadata = {
  title: "Patient Dashboard",
  description:
    "Monitor your health metrics, view alerts, and get AI-powered recommendations for better health outcomes.",
};

export default function PatientLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
